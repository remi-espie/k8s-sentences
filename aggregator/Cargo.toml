[package]
name = "aggregator"
version = "0.1.0"
edition = "2021"

[dependencies]
actix-web = "4.11.0"
hostname = "0.4.1"
reqwest = { version = "0.12.20", default-features = false, features = ["blocking", "json", "rustls-tls"] }
